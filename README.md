# mesh-preview

Mobile-friendly WebGL previewer for SingaLab knitting mesh outputs.

Open a project folder / manifest, orbit the mesh, use sliders for iterations and display modes. Target: Android Chrome.

