/**
 * The default demo is the real SingaLab cylinder dump in
 * public/sample/cylinder/. This script no longer writes a toy sleeve
 * over that folder.
 */
console.log(
  "Default sample is public/sample/cylinder/ (Single_Cylinder_Test). Not regenerating.",
);
